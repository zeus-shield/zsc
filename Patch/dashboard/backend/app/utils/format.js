'use strict';

// const enums = require('../../config/enum');
const moment = require('moment');

module.exports = {
  formatDate(date) {
    let result;
    if (!date) {
    } else {
    }
    return result;
  },
  user(data) {
  }
};