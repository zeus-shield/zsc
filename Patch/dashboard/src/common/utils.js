
const utils = {
  storage: {
    cookie: {
      set(key, value, expires) {
        if (expires === 'N/A') {
        } else {
        }
      },
      get(key) {
      },
      remove(key) {
      }
    }
  }
};

export default utils;
