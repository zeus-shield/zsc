// import shop from '../../apis/shop'

// initial state
const state = () => {
  return {
    insurance: {
    },
  };
};

// getters
const getters = {};

// mutations
const mutations = {};

// actions
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};