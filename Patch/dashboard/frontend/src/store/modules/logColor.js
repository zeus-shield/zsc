// import shop from '../../apis/shop'

// initial state
const state = () => {
  return {
    insurance: {
      base: 'black',
      pingan: {
        brief: 'black',
        detail: 'black'
      },
      picc: {
      }
    },
    admin: {
      company: {
      },
      insurance: {
      }
    }
  };
};

// getters
const getters = {};

// mutations
const mutations = {};

// actions
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};